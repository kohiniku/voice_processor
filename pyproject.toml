[project]
name = "voice-processor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-multipart",
    "openai-whisper",
    "qwen-asr",
    "pydantic-settings",
]
